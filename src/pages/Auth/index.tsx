import React from 'react';
import Register from './Register';

const Auth = () => {
    return (
        <div className={'auth'}>
            <div className={'auth__title'}>Cards</div>
            <Register/>
        </div>
    );
};

export default Auth;